# 계획 v3 — 웨이퍼 불량 패턴 탐지·판독 에이전트

SK하이닉스 AI 해커톤 2026 지원용 (접수 마감 **2026-09-28(월) 17:00**). 09-25 v3 — 주제를 "분류"에서 "아는 패턴 분류 + 처음 보는 패턴 탐지 + 검증된 판독"으로 넓혔다.

## 풀려는 문제

분류 모델은 학습 때 본 불량 패턴만 안다. 현장에서 곤란한 것은 **처음 보는 패턴**인데, 분류기는 그것을 아는 패턴 중 하나로 우겨 넣는다.
그래서 두 겹으로 만든다 — 아는 패턴은 분류하고, 모르는 패턴은 "처음 보는 것"으로 따로 올리고, 둘 다 **위치**와 **판독 리포트**를 붙인다. 판독은 자동 검사로 검증한다.

## 데이터

- WM-811K (MIR Lab) `data/raw/MIR-WM811K/Python/WM811K.pkl` — 811,457장, 라벨 약 17만(9종: Center·Donut·Edge-Loc·Edge-Ring·Loc·Near-full·Random·Scratch·none), 나머지 라벨 없음
- 칸: `dieSize` · `failureType` · `lotName` · `trainTestLabel` · `waferIndex` · `waferMap`(0 웨이퍼 밖 · 1 정상 다이 · 2 불량 다이)
- 인용 (이용 조건 — README 에 저작권 표시와 함께)
  - [1] M.-J. Wu, J.-S. R. Jang, J.-L. Chen, "Wafer Map Failure Pattern Recognition and Similarity Ranking for Large-Scale Data Sets," IEEE TSM 28(1), 2015. doi:10.1109/TSM.2014.2364237
  - [2] MIR-WM811K: Dataset for wafer map failure pattern recognition, 2015. http://mirlab.org/dataset/public/

## 구성

| # | 부분 | 하는 것 | 검증 (숫자) |
|---|---|---|---|
| 1 | 진단 | 라벨 분포 · 공식 분할의 로트 겹침 · 분할 간 똑같은 맵 · 같은 맵 다른 라벨 · 맵 크기 | `reports/eda.json` |
| 2 | 아는 패턴 분류 | 64×64 최근접 리사이즈(0/1/2 보존) · 로트 단위 분할 · CNN(+ 되면 ResNet18) · 클래스 가중치 | macro-F1 · 클래스별 재현율 · **공식 분할 대 로트 분할 격차** |
| 3 | 처음 보는 패턴 탐지 | 정상(`none`)과 아는 패턴으로 학습한 임베딩 위에 PatchCore 식 kNN 이상 점수 + 분류기 불확실도(최대 확률·에너지) 비교 · 웨이퍼 위 이상 히트맵 | **한 종류씩 빼고 학습 → 뺀 패턴을 '처음 보는 것'으로 잡는 AUROC / 오경보 5 %에서 재현율** (종류별 표) |
| 4 | 81만 장 지도·검색 | 임베딩 → UMAP 2차원 지도(패턴별 색, 라벨 없는 것 포함) · 새 웨이퍼의 유사 과거 사례 top-k | 검색 정밀도@k(라벨 있는 것으로) |
| 5 | 판독 에이전트 | 판정·히트맵 위치(중심/가장자리/링/선 — 코드가 계산)·유사 사례 → Ollama qwen3.5 가 원인 공정 후보·점검 순서 리포트. 원인은 문헌 표 안에서만 | **자동 검사 통과율**: 표 밖 원인 0 · 말한 위치 = 계산된 위치 · 판정과 모순 없음 |
| 6 | 화면 | 웨이퍼 올리면: 원본 | 히트맵 | 판정·"처음 보는 패턴" 경고 | 유사 사례 | 판독 카드 + 81만 장 지도 | 스크린샷 |
| 7 | 시험·정리 | pytest(로트 누수 없음 · 전처리 값 보존 · 지표 독립 계산 대조 · 위치 계산) · README · 포트폴리오 PDF 1장 · 자소서 문단 | 전부 통과 |

모자라면 뺄 순서: ResNet18 → 화면의 지도 대화형(정적 그림으로) → 시드 반복. **1·2·3·5·7 은 남긴다.**
여유가 있으면: 같은 탐지 엔진을 VisA PCB 에 적용("검사마다 새로 만들 필요 없는 틀").

## 일정

| 언제 | 할 일 |
|---|---|
| 09-25(금) | 1 진단 · 2 분할·CNN |
| 09-26(토) | 2 누수 격차 · 3 처음 보는 패턴 탐지(한 종류씩 빼기) |
| 09-27(일) | 4 지도·검색 · 5 에이전트 · 6 화면 |
| 09-28(월) 오전 | 7 정리 · PDF · 자소서 문단 · 17:00 전 제출 |

## 환경

- Python 3.14(`/c/Python314/python`) · torch 2.13+cu126 · RTX 3060 Ti 8GB · pandas 3.0.2(pkl 로드 8초) · Ollama qwen3.5:4b·9b
- 추가 패키지(umap-learn · scikit-learn · 필요하면 torchvision·faiss)는 **프로젝트 전용 `.venv`(`--system-site-packages`)** 에만. 전역 Python 은 건드리지 않는다. Python 3.14 에서 안 되는 패키지는 직접 구현하고 기록

## 규칙

- 수치는 실측만. 모르는 값은 `[측정 필요]`. 결과는 `reports/` 에 날짜와 함께, 틀렸던 측정도 남긴다
- 무거운 작업은 한 번에 하나. **이 프로젝트의 학습·LLM 실행은 09-25 사용자가 한 번에 허락** — 다른 GPU 작업(KAMP 등)이 돌고 있으면 멈추고 묻는다. 단계마다 결과를 보고한다
- `data/` 는 git 제외. 커밋은 단계마다, 경로 지정해서만. 원격 생성·push·공개는 하지 않는다
